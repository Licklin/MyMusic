<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMusic"
        tools:targetApi="31">

        <service android:name=".service.OurMusicService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowseService"/>
            </intent-filter>
        </service>

        <activity
            android:name=".TestMediaSession"
            android:exported="true"
            android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <service
            android:name=".service.MusicService"
            android:enabled="true"
            android:exported="true"
            android:stopWithTask="false" />

        <activity
            android:name=".ui.setting.api.EditActivity"
            android:exported="false" />
        <activity
            android:name=".ui.setting.api.APIActivity"
            android:exported="false" />
        <activity
            android:name=".ui.setting.notice.NoticeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.setting.sound_quality.SoundQualityActivity"
            android:exported="false" />
        <activity
            android:name=".ui.setting.home.SettingHomeActivity"
            android:exported="false" />
        <activity
            android:name=".ui.setting.password.PassWordActivity"
            android:exported="false" />
        <activity
            android:name=".ui.home.PQ.Desktop"
            android:exported="false" />
        <activity
            android:name=".ui.load.LoadActivity"
            android:exported="false">

        </activity>
    </application>

</manifest>